<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="post">
	<select id="selectByCategoryId" resultType="postvo" parameterType="int">
		<![CDATA[
			SELECT id, title, reg_date FROM posts WHERE category_id=#{id }
		]]>
	</select>
	<select id="selectByPostId" resultType="postvo" parameterType="int">
		<![CDATA[
			SELECT title, content, reg_date FROM posts WHERE id=#{id }
		]]>
	</select>
	<select id="selectNullPost" resultType="postvo" parameterType="int">
		<![CDATA[
			SELECT title, content, reg_date FROM posts WHERE id=14
		]]>
	</select>
	<select id="popPostId" resultType="int" parameterType="int">
		<![CDATA[
			Select id from posts where category_id=#{id} order by id desc limit 1;
		]]>
	</select>
</mapper>
